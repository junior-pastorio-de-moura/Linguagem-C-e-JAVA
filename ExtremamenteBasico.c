#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(){
    int a, b, x;
    scanf("%d %d", &a, &b);
    x = a+b;
    printf("x = %d\n", x);
    system("pause");
    return 0;
}