#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>
#include<locale.h>

int main(){
    system("clear");
    //Usar a definição padrão do sistema e imprimindo!
    puts(setlocale(LC_ALL, ""));
    printf("A calçada não está ótima!\n");
    return 0;
}